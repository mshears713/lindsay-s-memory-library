import PageContainer from "@/components/PageContainer";

const Chronicles = () => (
  <PageContainer title="Chronicles" subtitle="A timeline through the chapters of life">
    <div className="flex items-center justify-center min-h-[40vh] rounded-2xl border border-border bg-card/50">
      <p className="text-muted-foreground font-light italic">The timeline is being written…</p>
    </div>
  </PageContainer>
);

export default Chronicles;
