import PageContainer from "@/components/PageContainer";

const Reflections = () => (
  <PageContainer title="Reflections" subtitle="Mood-shifting spaces for quiet thought">
    <div className="flex items-center justify-center min-h-[40vh] rounded-2xl border border-border bg-card/50">
      <p className="text-muted-foreground font-light italic">Let the mood find you…</p>
    </div>
  </PageContainer>
);

export default Reflections;
